fastapi>=0.115,<0.116
uvicorn[standard]>=0.31,<0.32
pydantic>=2.9,<3
python-dotenv>=1.0,<2
websockets>=13,<14
avantis-trader-sdk
privy-client
httpx
pyjwt[crypto]>=2.9,<3
# pulled in transitively via web3 inside avantis-trader-sdk, listed
# here because usdc_approval.py imports them directly.
eth-abi
eth-utils

# Supabase persistence — trade history + leaderboard. Best-effort:
# when SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY are unset, persistence
# is a no-op and trades still execute normally.
supabase>=2.7,<3

# Error monitoring. Inert when SENTRY_DSN is unset, so this dep adds
# nothing in local dev where the env isn't set.
sentry-sdk[fastapi]>=2.18,<3
