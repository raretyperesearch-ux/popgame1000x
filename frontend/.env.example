NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_PRIVY_APP_ID=cmm1yn38100dm0cldx1mcej4t
# Privy authorization key ID — generated when a public PEM is registered
# in the Privy dashboard (App Settings -> Authorization Keys). Required for
# the addSigners() delegation flow on TEE wallets. Without this, users can
# log in but the backend can't sign trades on their behalf.
NEXT_PUBLIC_PRIVY_SIGNER_ID=
